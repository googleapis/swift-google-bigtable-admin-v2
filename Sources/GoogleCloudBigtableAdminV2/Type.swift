// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// `Type` represents the type of data that is written to, read from, or stored
/// in Bigtable. It is heavily based on the GoogleSQL standard to help maintain
/// familiarity and consistency across products and features.
///
/// For compatibility with Bigtable's existing untyped APIs, each `Type` includes
/// an `Encoding` which describes how to convert to or from the underlying data.
///
/// Each encoding can operate in one of two modes:
///
///  - Sorted: In this mode, Bigtable guarantees that `Encode(X) <= Encode(Y)`
///    if and only if `X <= Y`. This is useful anywhere sort order is important,
///    for example when encoding keys.
///  - Distinct: In this mode, Bigtable guarantees that if `X != Y` then
///   `Encode(X) != Encode(Y)`. However, the converse is not guaranteed. For
///    example, both "{'foo': '1', 'bar': '2'}" and "{'bar': '2', 'foo': '1'}"
///    are valid encodings of the same JSON value.
///
/// The API clearly documents which mode is used wherever an encoding can be
/// configured. Each encoding also documents which values are supported in which
/// modes. For example, when encoding INT64 as a numeric STRING, negative numbers
/// cannot be encoded in sorted mode. This is because `INT64(1) > INT64(-1)`, but
/// `STRING("-00001") > STRING("00001")`.
public struct Type_: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// The kind of type that this represents.
  public var kind: OneOf_Kind? = nil

  /// Initialize a new instance of `Type_`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Type_().with { $0.bytesType = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case bytesType = "bytesType"
    case stringType = "stringType"
    case int64Type = "int64Type"
    case float32Type = "float32Type"
    case float64Type = "float64Type"
    case boolType = "boolType"
    case timestampType = "timestampType"
    case dateType = "dateType"
    case aggregateType = "aggregateType"
    case structType = "structType"
    case arrayType = "arrayType"
    case mapType = "mapType"
    case protoType = "protoType"
    case enumType = "enumType"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)

    var kind: OneOf_Kind? = nil
    let kindCheckAndSet = {
      if kind != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath, debugDescription: "Multiple values set for oneof 'kind'"
          ))
      }
      kind = $0
    }
    if let bytesType = try container.decodeIfPresent(Type_.Bytes?.self, forKey: .bytesType) {
      try kindCheckAndSet(.bytesType(bytesType))
    }
    if let stringType = try container.decodeIfPresent(Type_.String?.self, forKey: .stringType) {
      try kindCheckAndSet(.stringType(stringType))
    }
    if let int64Type = try container.decodeIfPresent(Type_.Int64?.self, forKey: .int64Type) {
      try kindCheckAndSet(.int64Type(int64Type))
    }
    if let float32Type = try container.decodeIfPresent(Type_.Float32?.self, forKey: .float32Type) {
      try kindCheckAndSet(.float32Type(float32Type))
    }
    if let float64Type = try container.decodeIfPresent(Type_.Float64?.self, forKey: .float64Type) {
      try kindCheckAndSet(.float64Type(float64Type))
    }
    if let boolType = try container.decodeIfPresent(Type_.Bool?.self, forKey: .boolType) {
      try kindCheckAndSet(.boolType(boolType))
    }
    if let timestampType = try container.decodeIfPresent(
      Type_.Timestamp?.self, forKey: .timestampType)
    {
      try kindCheckAndSet(.timestampType(timestampType))
    }
    if let dateType = try container.decodeIfPresent(Type_.Date?.self, forKey: .dateType) {
      try kindCheckAndSet(.dateType(dateType))
    }
    if let aggregateType = try container.decodeIfPresent(
      Type_.Aggregate?.self, forKey: .aggregateType)
    {
      try kindCheckAndSet(.aggregateType(aggregateType))
    }
    if let structType = try container.decodeIfPresent(Type_.Struct?.self, forKey: .structType) {
      try kindCheckAndSet(.structType(structType))
    }
    if let arrayType = try container.decodeIfPresent(Type_.Array?.self, forKey: .arrayType) {
      try kindCheckAndSet(.arrayType(arrayType))
    }
    if let mapType = try container.decodeIfPresent(Type_.Map?.self, forKey: .mapType) {
      try kindCheckAndSet(.mapType(mapType))
    }
    if let protoType = try container.decodeIfPresent(Type_.Proto?.self, forKey: .protoType) {
      try kindCheckAndSet(.protoType(protoType))
    }
    if let enumType = try container.decodeIfPresent(Type_.Enum?.self, forKey: .enumType) {
      try kindCheckAndSet(.enumType(enumType))
    }
    self.kind = kind
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)

    if let choice = self.kind {
      switch choice {
      case .bytesType(let value):
        try container.encode(value, forKey: .bytesType)
      case .stringType(let value):
        try container.encode(value, forKey: .stringType)
      case .int64Type(let value):
        try container.encode(value, forKey: .int64Type)
      case .float32Type(let value):
        try container.encode(value, forKey: .float32Type)
      case .float64Type(let value):
        try container.encode(value, forKey: .float64Type)
      case .boolType(let value):
        try container.encode(value, forKey: .boolType)
      case .timestampType(let value):
        try container.encode(value, forKey: .timestampType)
      case .dateType(let value):
        try container.encode(value, forKey: .dateType)
      case .aggregateType(let value):
        try container.encode(value, forKey: .aggregateType)
      case .structType(let value):
        try container.encode(value, forKey: .structType)
      case .arrayType(let value):
        try container.encode(value, forKey: .arrayType)
      case .mapType(let value):
        try container.encode(value, forKey: .mapType)
      case .protoType(let value):
        try container.encode(value, forKey: .protoType)
      case .enumType(let value):
        try container.encode(value, forKey: .enumType)
      }
    }
  }

  /// Bytes
  /// Values of type `Bytes` are stored in `Value.bytes_value`.
  public struct Bytes: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The encoding to use when converting to or from lower level types.
    public var encoding: Type_.Bytes.Encoding? = nil

    /// Initialize a new instance of `Bytes`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Bytes().with { $0.encoding = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Rules used to convert to or from lower level types.
    public struct Encoding: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Which encoding to use.
      public var encoding: OneOf_Encoding? = nil

      /// Initialize a new instance of `Encoding`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Encoding().with { $0.raw = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case raw = "raw"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var encoding: OneOf_Encoding? = nil
        let encodingCheckAndSet = {
          if encoding != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'encoding'"))
          }
          encoding = $0
        }
        if let raw = try container.decodeIfPresent(Type_.Bytes.Encoding.Raw?.self, forKey: .raw) {
          try encodingCheckAndSet(.raw(raw))
        }
        self.encoding = encoding
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.encoding {
          switch choice {
          case .raw(let value):
            try container.encode(value, forKey: .raw)
          }
        }
      }

      /// Leaves the value as-is.
      ///
      /// Sorted mode: all values are supported.
      ///
      /// Distinct mode: all values are supported.
      public struct Raw: Codable, Equatable, GoogleCloudWKT._AnyPackable,
        Sendable
      {
        /// Initialize a new instance of `Raw`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Raw().with { $0.<placeholder> = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.bigtable.admin.v2.Type.Bytes.Encoding.Raw"
        }
        public init(fromAny any: GoogleCloudWKT.`Any`) throws {
          self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWKT.Struct {
          return try GoogleCloudWKT._slowAnySerialize(message: self)
        }
      }

      /// Which encoding to use.
      public enum OneOf_Encoding: Codable, Equatable, Sendable {
        /// Use `Raw` encoding.
        indirect case raw(Type_.Bytes.Encoding.Raw?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.bigtable.admin.v2.Type.Bytes.Encoding"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Bytes"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// String
  /// Values of type `String` are stored in `Value.string_value`.
  public struct String: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The encoding to use when converting to or from lower level types.
    public var encoding: Type_.String.Encoding? = nil

    /// Initialize a new instance of `String`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = String().with { $0.encoding = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Rules used to convert to or from lower level types.
    public struct Encoding: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Which encoding to use.
      public var encoding: OneOf_Encoding? = nil

      /// Initialize a new instance of `Encoding`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Encoding().with { $0.utf8Raw = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case utf8Raw = "utf8Raw"
        case utf8Bytes = "utf8Bytes"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var encoding: OneOf_Encoding? = nil
        let encodingCheckAndSet = {
          if encoding != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'encoding'"))
          }
          encoding = $0
        }
        if let utf8Raw = try container.decodeIfPresent(
          Type_.String.Encoding.Utf8Raw?.self, forKey: .utf8Raw)
        {
          try encodingCheckAndSet(.utf8Raw(utf8Raw))
        }
        if let utf8Bytes = try container.decodeIfPresent(
          Type_.String.Encoding.Utf8Bytes?.self, forKey: .utf8Bytes)
        {
          try encodingCheckAndSet(.utf8Bytes(utf8Bytes))
        }
        self.encoding = encoding
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.encoding {
          switch choice {
          case .utf8Raw(let value):
            try container.encode(value, forKey: .utf8Raw)
          case .utf8Bytes(let value):
            try container.encode(value, forKey: .utf8Bytes)
          }
        }
      }

      /// Deprecated: prefer the equivalent `Utf8Bytes`.
      @available(*, deprecated)
      public struct Utf8Raw: Codable, Equatable, GoogleCloudWKT._AnyPackable,
        Sendable
      {
        /// Initialize a new instance of `Utf8Raw`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Utf8Raw().with { $0.<placeholder> = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.bigtable.admin.v2.Type.String.Encoding.Utf8Raw"
        }
        public init(fromAny any: GoogleCloudWKT.`Any`) throws {
          self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWKT.Struct {
          return try GoogleCloudWKT._slowAnySerialize(message: self)
        }
      }

      /// UTF-8 encoding.
      ///
      /// Sorted mode:
      ///  - All values are supported.
      ///  - Code point order is preserved.
      ///
      /// Distinct mode: all values are supported.
      ///
      /// Compatible with:
      ///
      ///  - BigQuery `TEXT` encoding
      ///  - HBase `Bytes.toBytes`
      ///  - Java `String#getBytes(StandardCharsets.UTF_8)`
      public struct Utf8Bytes: Codable, Equatable, GoogleCloudWKT._AnyPackable,
        Sendable
      {
        /// Initialize a new instance of `Utf8Bytes`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Utf8Bytes().with { $0.<placeholder> = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.bigtable.admin.v2.Type.String.Encoding.Utf8Bytes"
        }
        public init(fromAny any: GoogleCloudWKT.`Any`) throws {
          self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWKT.Struct {
          return try GoogleCloudWKT._slowAnySerialize(message: self)
        }
      }

      /// Which encoding to use.
      public enum OneOf_Encoding: Codable, Equatable, Sendable {
        /// Deprecated: if set, converts to an empty `utf8_bytes`.
        @available(*, deprecated)
        indirect case utf8Raw(Type_.String.Encoding.Utf8Raw?)
        /// Use `Utf8Bytes` encoding.
        indirect case utf8Bytes(Type_.String.Encoding.Utf8Bytes?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.bigtable.admin.v2.Type.String.Encoding"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.String"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Int64
  /// Values of type `Int64` are stored in `Value.int_value`.
  public struct Int64: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The encoding to use when converting to or from lower level types.
    public var encoding: Type_.Int64.Encoding? = nil

    /// Initialize a new instance of `Int64`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Int64().with { $0.encoding = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Rules used to convert to or from lower level types.
    public struct Encoding: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Which encoding to use.
      public var encoding: OneOf_Encoding? = nil

      /// Initialize a new instance of `Encoding`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Encoding().with { $0.bigEndianBytes = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case bigEndianBytes = "bigEndianBytes"
        case orderedCodeBytes = "orderedCodeBytes"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var encoding: OneOf_Encoding? = nil
        let encodingCheckAndSet = {
          if encoding != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'encoding'"))
          }
          encoding = $0
        }
        if let bigEndianBytes = try container.decodeIfPresent(
          Type_.Int64.Encoding.BigEndianBytes?.self, forKey: .bigEndianBytes)
        {
          try encodingCheckAndSet(.bigEndianBytes(bigEndianBytes))
        }
        if let orderedCodeBytes = try container.decodeIfPresent(
          Type_.Int64.Encoding.OrderedCodeBytes?.self, forKey: .orderedCodeBytes)
        {
          try encodingCheckAndSet(.orderedCodeBytes(orderedCodeBytes))
        }
        self.encoding = encoding
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.encoding {
          switch choice {
          case .bigEndianBytes(let value):
            try container.encode(value, forKey: .bigEndianBytes)
          case .orderedCodeBytes(let value):
            try container.encode(value, forKey: .orderedCodeBytes)
          }
        }
      }

      /// Encodes the value as an 8-byte big-endian two's complement value.
      ///
      /// Sorted mode: non-negative values are supported.
      ///
      /// Distinct mode: all values are supported.
      ///
      /// Compatible with:
      ///
      ///  - BigQuery `BINARY` encoding
      ///  - HBase `Bytes.toBytes`
      ///  - Java `ByteBuffer.putLong()` with `ByteOrder.BIG_ENDIAN`
      public struct BigEndianBytes: Codable, Equatable, GoogleCloudWKT._AnyPackable,
        Sendable
      {
        /// Deprecated: ignored if set.
        @available(*, deprecated)
        public var bytesType: Type_.Bytes? = nil

        /// Initialize a new instance of `BigEndianBytes`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = BigEndianBytes().with { $0.bytesType = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.bigtable.admin.v2.Type.Int64.Encoding.BigEndianBytes"
        }
        public init(fromAny any: GoogleCloudWKT.`Any`) throws {
          self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWKT.Struct {
          return try GoogleCloudWKT._slowAnySerialize(message: self)
        }
      }

      /// Encodes the value in a variable length binary format of up to 10 bytes.
      /// Values that are closer to zero use fewer bytes.
      ///
      /// Sorted mode: all values are supported.
      ///
      /// Distinct mode: all values are supported.
      public struct OrderedCodeBytes: Codable, Equatable, GoogleCloudWKT._AnyPackable,
        Sendable
      {
        /// Initialize a new instance of `OrderedCodeBytes`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = OrderedCodeBytes().with { $0.<placeholder> = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.bigtable.admin.v2.Type.Int64.Encoding.OrderedCodeBytes"
        }
        public init(fromAny any: GoogleCloudWKT.`Any`) throws {
          self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWKT.Struct {
          return try GoogleCloudWKT._slowAnySerialize(message: self)
        }
      }

      /// Which encoding to use.
      public enum OneOf_Encoding: Codable, Equatable, Sendable {
        /// Use `BigEndianBytes` encoding.
        indirect case bigEndianBytes(Type_.Int64.Encoding.BigEndianBytes?)
        /// Use `OrderedCodeBytes` encoding.
        indirect case orderedCodeBytes(Type_.Int64.Encoding.OrderedCodeBytes?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.bigtable.admin.v2.Type.Int64.Encoding"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Int64"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// bool
  /// Values of type `Bool` are stored in `Value.bool_value`.
  public struct Bool: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `Bool`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Bool().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Bool"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Float32
  /// Values of type `Float32` are stored in `Value.float_value`.
  public struct Float32: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `Float32`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Float32().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Float32"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Float64
  /// Values of type `Float64` are stored in `Value.float_value`.
  public struct Float64: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `Float64`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Float64().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Float64"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Timestamp
  /// Values of type `Timestamp` are stored in `Value.timestamp_value`.
  public struct Timestamp: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The encoding to use when converting to or from lower level types.
    public var encoding: Type_.Timestamp.Encoding? = nil

    /// Initialize a new instance of `Timestamp`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Timestamp().with { $0.encoding = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Rules used to convert to or from lower level types.
    public struct Encoding: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Which encoding to use.
      public var encoding: OneOf_Encoding? = nil

      /// Initialize a new instance of `Encoding`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Encoding().with { $0.unixMicrosInt64 = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case unixMicrosInt64 = "unixMicrosInt64"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var encoding: OneOf_Encoding? = nil
        let encodingCheckAndSet = {
          if encoding != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'encoding'"))
          }
          encoding = $0
        }
        if let unixMicrosInt64 = try container.decodeIfPresent(
          Type_.Int64.Encoding?.self, forKey: .unixMicrosInt64)
        {
          try encodingCheckAndSet(.unixMicrosInt64(unixMicrosInt64))
        }
        self.encoding = encoding
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.encoding {
          switch choice {
          case .unixMicrosInt64(let value):
            try container.encode(value, forKey: .unixMicrosInt64)
          }
        }
      }

      /// Which encoding to use.
      public enum OneOf_Encoding: Codable, Equatable, Sendable {
        /// Encodes the number of microseconds since the Unix epoch using the
        /// given `Int64` encoding. Values must be microsecond-aligned.
        ///
        /// Compatible with:
        ///
        ///  - Java `Instant.truncatedTo()` with `ChronoUnit.MICROS`
        indirect case unixMicrosInt64(Type_.Int64.Encoding?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.bigtable.admin.v2.Type.Timestamp.Encoding"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Timestamp"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Date
  /// Values of type `Date` are stored in `Value.date_value`.
  public struct Date: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `Date`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Date().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Date"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// A structured data value, consisting of fields which map to dynamically
  /// typed values.
  /// Values of type `Struct` are stored in `Value.array_value` where entries are
  /// in the same order and number as `field_types`.
  public struct Struct: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The names and types of the fields in this struct.
    public var fields: [Type_.Struct.Field] = []

    /// The encoding to use when converting to or from lower level types.
    public var encoding: Type_.Struct.Encoding? = nil

    /// Initialize a new instance of `Struct`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Struct().with { $0.fields = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// A struct field and its type.
    public struct Field: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// The field name (optional). Fields without a `field_name` are considered
      /// anonymous and cannot be referenced by name.
      public var fieldName: Swift.String = Swift.String()

      /// The type of values in this field.
      public var type: GoogleCloudWKT.Recursive<Type_>? = nil

      /// Initialize a new instance of `Field`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Field().with { $0.fieldName = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.bigtable.admin.v2.Type.Struct.Field"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// Rules used to convert to or from lower level types.
    public struct Encoding: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Which encoding to use.
      public var encoding: OneOf_Encoding? = nil

      /// Initialize a new instance of `Encoding`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Encoding().with { $0.singleton = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case singleton = "singleton"
        case delimitedBytes = "delimitedBytes"
        case orderedCodeBytes = "orderedCodeBytes"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var encoding: OneOf_Encoding? = nil
        let encodingCheckAndSet = {
          if encoding != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'encoding'"))
          }
          encoding = $0
        }
        if let singleton = try container.decodeIfPresent(
          Type_.Struct.Encoding.Singleton?.self, forKey: .singleton)
        {
          try encodingCheckAndSet(.singleton(singleton))
        }
        if let delimitedBytes = try container.decodeIfPresent(
          Type_.Struct.Encoding.DelimitedBytes?.self, forKey: .delimitedBytes)
        {
          try encodingCheckAndSet(.delimitedBytes(delimitedBytes))
        }
        if let orderedCodeBytes = try container.decodeIfPresent(
          Type_.Struct.Encoding.OrderedCodeBytes?.self, forKey: .orderedCodeBytes)
        {
          try encodingCheckAndSet(.orderedCodeBytes(orderedCodeBytes))
        }
        self.encoding = encoding
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.encoding {
          switch choice {
          case .singleton(let value):
            try container.encode(value, forKey: .singleton)
          case .delimitedBytes(let value):
            try container.encode(value, forKey: .delimitedBytes)
          case .orderedCodeBytes(let value):
            try container.encode(value, forKey: .orderedCodeBytes)
          }
        }
      }

      /// Uses the encoding of `fields[0].type` as-is.
      /// Only valid if `fields.size == 1`.
      public struct Singleton: Codable, Equatable, GoogleCloudWKT._AnyPackable,
        Sendable
      {
        /// Initialize a new instance of `Singleton`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Singleton().with { $0.<placeholder> = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.bigtable.admin.v2.Type.Struct.Encoding.Singleton"
        }
        public init(fromAny any: GoogleCloudWKT.`Any`) throws {
          self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWKT.Struct {
          return try GoogleCloudWKT._slowAnySerialize(message: self)
        }
      }

      /// Fields are encoded independently and concatenated with a configurable
      /// `delimiter` in between.
      ///
      /// A struct with no fields defined is encoded as a single `delimiter`.
      ///
      /// Sorted mode:
      ///
      ///  - Fields are encoded in sorted mode.
      ///  - Encoded field values must not contain any bytes <= `delimiter[0]`
      ///  - Element-wise order is preserved: `A < B` if `A[0] < B[0]`, or if
      ///    `A[0] == B[0] && A[1] < B[1]`, etc. Strict prefixes sort first.
      ///
      /// Distinct mode:
      ///
      ///  - Fields are encoded in distinct mode.
      ///  - Encoded field values must not contain `delimiter[0]`.
      public struct DelimitedBytes: Codable, Equatable, GoogleCloudWKT._AnyPackable,
        Sendable
      {
        /// Byte sequence used to delimit concatenated fields. The delimiter must
        /// contain at least 1 character and at most 50 characters.
        public var delimiter: Foundation.Data = Foundation.Data()

        /// Initialize a new instance of `DelimitedBytes`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = DelimitedBytes().with { $0.delimiter = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.bigtable.admin.v2.Type.Struct.Encoding.DelimitedBytes"
        }
        public init(fromAny any: GoogleCloudWKT.`Any`) throws {
          self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWKT.Struct {
          return try GoogleCloudWKT._slowAnySerialize(message: self)
        }
      }

      /// Fields are encoded independently and concatenated with the fixed byte
      /// pair {0x00, 0x01} in between.
      ///
      /// Any null (0x00) byte in an encoded field is replaced by the fixed byte
      /// pair {0x00, 0xFF}.
      ///
      /// Fields that encode to the empty string "" have special handling:
      ///
      ///  - If *every* field encodes to "", or if the STRUCT has no fields
      ///    defined, then the STRUCT is encoded as the fixed byte pair
      ///    {0x00, 0x00}.
      ///  - Otherwise, the STRUCT only encodes until the last non-empty field,
      ///    omitting any trailing empty fields. Any empty fields that aren't
      ///    omitted are replaced with the fixed byte pair {0x00, 0x00}.
      ///
      /// Examples:
      ///
      ///  - STRUCT()             -> "\00\00"
      ///  - STRUCT("")           -> "\00\00"
      ///  - STRUCT("", "")       -> "\00\00"
      ///  - STRUCT("", "B")      -> "\00\00" + "\00\01" + "B"
      ///  - STRUCT("A", "")      -> "A"
      ///  - STRUCT("", "B", "")  -> "\00\00" + "\00\01" + "B"
      ///  - STRUCT("A", "", "C") -> "A" + "\00\01" + "\00\00" + "\00\01" + "C"
      ///
      ///
      /// Since null bytes are always escaped, this encoding can cause size
      /// blowup for encodings like `Int64.BigEndianBytes` that are likely to
      /// produce many such bytes.
      ///
      /// Sorted mode:
      ///
      ///  - Fields are encoded in sorted mode.
      ///  - All values supported by the field encodings are allowed
      ///  - Element-wise order is preserved: `A < B` if `A[0] < B[0]`, or if
      ///    `A[0] == B[0] && A[1] < B[1]`, etc. Strict prefixes sort first.
      ///
      /// Distinct mode:
      ///
      ///  - Fields are encoded in distinct mode.
      ///  - All values supported by the field encodings are allowed.
      public struct OrderedCodeBytes: Codable, Equatable, GoogleCloudWKT._AnyPackable,
        Sendable
      {
        /// Initialize a new instance of `OrderedCodeBytes`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = OrderedCodeBytes().with { $0.<placeholder> = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.bigtable.admin.v2.Type.Struct.Encoding.OrderedCodeBytes"
        }
        public init(fromAny any: GoogleCloudWKT.`Any`) throws {
          self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWKT.Struct {
          return try GoogleCloudWKT._slowAnySerialize(message: self)
        }
      }

      /// Which encoding to use.
      public enum OneOf_Encoding: Codable, Equatable, Sendable {
        /// Use `Singleton` encoding.
        indirect case singleton(Type_.Struct.Encoding.Singleton?)
        /// Use `DelimitedBytes` encoding.
        indirect case delimitedBytes(Type_.Struct.Encoding.DelimitedBytes?)
        /// User `OrderedCodeBytes` encoding.
        indirect case orderedCodeBytes(Type_.Struct.Encoding.OrderedCodeBytes?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.bigtable.admin.v2.Type.Struct.Encoding"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Struct"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// A protobuf message type.
  /// Values of type `Proto` are stored in `Value.bytes_value`.
  public struct Proto: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The ID of the schema bundle that this proto is defined in.
    public var schemaBundleId: Swift.String = Swift.String()

    /// The fully qualified name of the protobuf message, including package. In
    /// the format of "foo.bar.Message".
    public var messageName: Swift.String = Swift.String()

    /// Initialize a new instance of `Proto`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Proto().with { $0.schemaBundleId = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Proto"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// A protobuf enum type.
  /// Values of type `Enum` are stored in `Value.int_value`.
  public struct Enum: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The ID of the schema bundle that this enum is defined in.
    public var schemaBundleId: Swift.String = Swift.String()

    /// The fully qualified name of the protobuf enum message, including package.
    /// In the format of "foo.bar.EnumMessage".
    public var enumName: Swift.String = Swift.String()

    /// Initialize a new instance of `Enum`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Enum().with { $0.schemaBundleId = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Enum"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// An ordered list of elements of a given type.
  /// Values of type `Array` are stored in `Value.array_value`.
  public struct Array: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The type of the elements in the array. This must not be `Array`.
    public var elementType: GoogleCloudWKT.Recursive<Type_>? = nil

    /// Initialize a new instance of `Array`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Array().with { $0.elementType = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Array"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// A mapping of keys to values of a given type.
  /// Values of type `Map` are stored in a `Value.array_value` where each entry
  /// is another `Value.array_value` with two elements (the key and the value,
  /// in that order).
  /// Normally encoded Map values won't have repeated keys, however, clients are
  /// expected to handle the case in which they do. If the same key appears
  /// multiple times, the _last_ value takes precedence.
  public struct Map: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The type of a map key.
    /// Only `Bytes`, `String`, and `Int64` are allowed as key types.
    public var keyType: GoogleCloudWKT.Recursive<Type_>? = nil

    /// The type of the values in a map.
    public var valueType: GoogleCloudWKT.Recursive<Type_>? = nil

    /// Initialize a new instance of `Map`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Map().with { $0.keyType = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Map"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// A value that combines incremental updates into a summarized value.
  ///
  /// Data is never directly written or read using type `Aggregate`. Writes will
  /// provide either the `input_type` or `state_type`, and reads will always
  /// return the `state_type` .
  public struct Aggregate: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Type of the inputs that are accumulated by this `Aggregate`, which must
    /// specify a full encoding.
    /// Use `AddInput` mutations to accumulate new inputs.
    public var inputType: GoogleCloudWKT.Recursive<Type_>? = nil

    /// Output only. Type that holds the internal accumulator state for the
    /// `Aggregate`. This is a function of the `input_type` and `aggregator`
    /// chosen, and will always specify a full encoding.
    public var stateType: GoogleCloudWKT.Recursive<Type_>? = nil

    /// Which aggregator function to use. The configured types must match.
    public var aggregator: OneOf_Aggregator? = nil

    /// Initialize a new instance of `Aggregate`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Aggregate().with { $0.inputType = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case inputType = "inputType"
      case stateType = "stateType"
      case sum = "sum"
      case hllppUniqueCount = "hllppUniqueCount"
      case max = "max"
      case min = "min"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.inputType = try container.decodeIfPresent(
        GoogleCloudWKT.Recursive<Type_>.self, forKey: .inputType)
      self.stateType = try container.decodeIfPresent(
        GoogleCloudWKT.Recursive<Type_>.self, forKey: .stateType)

      var aggregator: OneOf_Aggregator? = nil
      let aggregatorCheckAndSet = {
        if aggregator != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'aggregator'"))
        }
        aggregator = $0
      }
      if let sum = try container.decodeIfPresent(Type_.Aggregate.Sum?.self, forKey: .sum) {
        try aggregatorCheckAndSet(.sum(sum))
      }
      if let hllppUniqueCount = try container.decodeIfPresent(
        Type_.Aggregate.HyperLogLogPlusPlusUniqueCount?.self, forKey: .hllppUniqueCount)
      {
        try aggregatorCheckAndSet(.hllppUniqueCount(hllppUniqueCount))
      }
      if let max = try container.decodeIfPresent(Type_.Aggregate.Max?.self, forKey: .max) {
        try aggregatorCheckAndSet(.max(max))
      }
      if let min = try container.decodeIfPresent(Type_.Aggregate.Min?.self, forKey: .min) {
        try aggregatorCheckAndSet(.min(min))
      }
      self.aggregator = aggregator
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.inputType, forKey: .inputType)
      try container.encode(self.stateType, forKey: .stateType)

      if let choice = self.aggregator {
        switch choice {
        case .sum(let value):
          try container.encode(value, forKey: .sum)
        case .hllppUniqueCount(let value):
          try container.encode(value, forKey: .hllppUniqueCount)
        case .max(let value):
          try container.encode(value, forKey: .max)
        case .min(let value):
          try container.encode(value, forKey: .min)
        }
      }
    }

    /// Computes the sum of the input values.
    /// Allowed input: `Int64`
    /// State: same as input
    public struct Sum: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Initialize a new instance of `Sum`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Sum().with { $0.<placeholder> = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.bigtable.admin.v2.Type.Aggregate.Sum"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// Computes the max of the input values.
    /// Allowed input: `Int64`
    /// State: same as input
    public struct Max: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Initialize a new instance of `Max`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Max().with { $0.<placeholder> = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.bigtable.admin.v2.Type.Aggregate.Max"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// Computes the min of the input values.
    /// Allowed input: `Int64`
    /// State: same as input
    public struct Min: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Initialize a new instance of `Min`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Min().with { $0.<placeholder> = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.bigtable.admin.v2.Type.Aggregate.Min"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// Computes an approximate unique count over the input values. When using
    /// raw data as input, be careful to use a consistent encoding. Otherwise
    /// the same value encoded differently could count more than once, or two
    /// distinct values could count as identical.
    /// Input: Any, or omit for Raw
    /// State: TBD
    /// Special state conversions: `Int64` (the unique count estimate)
    public struct HyperLogLogPlusPlusUniqueCount: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Initialize a new instance of `HyperLogLogPlusPlusUniqueCount`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = HyperLogLogPlusPlusUniqueCount().with { $0.<placeholder> = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.bigtable.admin.v2.Type.Aggregate.HyperLogLogPlusPlusUniqueCount"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// Which aggregator function to use. The configured types must match.
    public enum OneOf_Aggregator: Codable, Equatable, Sendable {
      /// Sum aggregator.
      indirect case sum(Type_.Aggregate.Sum?)
      /// HyperLogLogPlusPlusUniqueCount aggregator.
      indirect case hllppUniqueCount(Type_.Aggregate.HyperLogLogPlusPlusUniqueCount?)
      /// Max aggregator.
      indirect case max(Type_.Aggregate.Max?)
      /// Min aggregator.
      indirect case min(Type_.Aggregate.Min?)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.bigtable.admin.v2.Type.Aggregate"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The kind of type that this represents.
  public enum OneOf_Kind: Codable, Equatable, Sendable {
    /// Bytes
    indirect case bytesType(Type_.Bytes?)
    /// String
    indirect case stringType(Type_.String?)
    /// Int64
    indirect case int64Type(Type_.Int64?)
    /// Float32
    indirect case float32Type(Type_.Float32?)
    /// Float64
    indirect case float64Type(Type_.Float64?)
    /// Bool
    indirect case boolType(Type_.Bool?)
    /// Timestamp
    indirect case timestampType(Type_.Timestamp?)
    /// Date
    indirect case dateType(Type_.Date?)
    /// Aggregate
    indirect case aggregateType(Type_.Aggregate?)
    /// Struct
    indirect case structType(Type_.Struct?)
    /// Array
    indirect case arrayType(Type_.Array?)
    /// Map
    indirect case mapType(Type_.Map?)
    /// Proto
    indirect case protoType(Type_.Proto?)
    /// Enum
    indirect case enumType(Type_.Enum?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.bigtable.admin.v2.Type"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
